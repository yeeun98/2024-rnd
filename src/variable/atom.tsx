import { atom } from "recoil";

export const isViewGolfzonSection = atom({
  key: 'isViewGolfzon',
  default: false
});
export const isSecondView = atom({
  key: 'isSecondView',
  default: false
});
export const isThirdView = atom({
  key: 'isThirdView',
  default: false
});